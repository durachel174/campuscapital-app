[phases.setup]
nixPkgs = ["python311", "postgresql"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "python scripts/startup.py && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
